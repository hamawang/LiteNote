{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Settings webview — locale, opacity, clock section",
  "windows": ["settings"],
  "permissions": []
}
